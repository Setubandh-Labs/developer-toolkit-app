import 'package:flutter/material.dart';

class Base64Page extends StatelessWidget {
  const Base64Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Base64 Encoder/Decoder'),
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}